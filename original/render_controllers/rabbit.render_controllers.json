{
  "format_version": "1.8.0",
  "render_controllers": {
    "controller.render.rabbit": {
      "arrays": {
        "textures": {
          "Array.skins": [
            "Texture.brown",
            "Texture.white",
            "Texture.black",
            "Texture.white_splotched",
            "Texture.gold",
            "Texture.salt"
          ]
        }
      },
      "geometry": "q.get_name == 'Jumbo' ? geometry.jumbo : (query.is_baby ? geometry.default_b : Geometry.default)",
      "materials": [
        {
          "*": "Material.default"
        }
      ],
      "textures": [
        "query.get_name == 'Toast' ? Texture.toast : (query.get_name == 'Vested' ? Texture.vested : (query.get_name == 'Bold' ? Texture.bold : (query.get_name == 'Freckled' ? Texture.freckled : (query.get_name == 'Harelequin' ? Texture.harelequin : (query.get_name == 'Muddy' ? Texture.muddy : (query.get_name == 'Jumbo' ? Texture.jumbo : Array.skins[query.variant]))))))"
      ]
    }
  }
}